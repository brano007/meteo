import { CloudConditionPipe } from './cloud-condition.pipe';

describe('CloudConditionPipe', () => {
  it('create an instance', () => {
    const pipe = new CloudConditionPipe();
    expect(pipe).toBeTruthy();
  });
});
